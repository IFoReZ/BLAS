#include <iostream>
#include "TVectorCls.h"
#include "TMatrixCls.h"

int main()
{
  /*
    setlocale(0, "");
    TMatrix<int> A(2,2);
    TMatrix<int> B(5,7);
    B[0] = 2.0;
    B[1] = 9.0;
    A.SaveToFile("data.txt");
    A.ReadFromFile("data.txt");

    TMatrix<int> C(0,0);
    C = A + B;
    std::cout << C << std::endl;
  */
  return 0;
}